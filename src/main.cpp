#include <iostream>
// #include "Orbit.hpp"     // you'll create this later

int main() {
    std::cout << "Satellite orbit simulator\n";

    // TODO: initialize Earth, satellite, time step, etc.
    // Example placeholders:
    //   Orbit earthOrbit(earthMass, satelliteMass, initialPosition, initialVelocity);
    //   while(simulationRunning) { earthOrbit.step(dt); }

    return 0;
}